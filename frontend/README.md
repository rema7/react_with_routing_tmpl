Repo for RME Assistant UI.

# Before

    $ npm install

# Run dev server (connect to localhost:9000)

    $ npm run dev:server

# Build prod version

    $ npm run build:prod

In created dir `dist` get dist.zip
